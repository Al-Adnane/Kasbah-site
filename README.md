# Kasbah — Final Final (Hook) — Cloudflare Pages Hybrid

## Deploy
1) Push this folder to GitHub.
2) Cloudflare → Pages → Create project → Connect repo.
3) Framework preset: None
4) Build command: (empty)
5) Output directory: public
6) Deploy.

## KV (recommended)
Create KV namespace `KASBAH_WAITLIST`, then bind it in Pages → Settings → Functions → KV bindings as `KASBAH_WAITLIST`.
